PACKAGE_NAME="cp210x-pps"
PACKAGE_VERSION="1.0.0"

BUILT_MODULE_NAME[0]="cp210x"
# Recorded origin path; on Ubuntu the built module is installed under
# /lib/modules/<kver>/updates/dkms/ which overrides the in-tree driver.
DEST_MODULE_LOCATION[0]="/kernel/drivers/usb/serial"

AUTOINSTALL="yes"
